const {add} =  require('./util/methods');
const {mult} = require('./util/methods');
const {sub} = require('./util/methods');
const {div} = require('./util/methods');
const sum = add(1 , 2, 3 , 4);
console.log(sum);